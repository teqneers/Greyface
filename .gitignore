greyface.ini

# /var/
/var/cache/*
!/var/cache/.gitignore
/var/log/*
!/var/log/.gitignore

/data

# The docker/ directory is committed. Only its generated output is not:
# docker/build/webserver holds the TLS key pair the Encore dev-server reads
# (see app/webpack.config.js), which is generated locally, never shared.
/docker/build/

## PhpStorm ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm
/*.iml

## Directory-based project format:
.idea/

## File-based project format:
*.ipr
*.iws

# Mac specific files
.DS_Store
._.DS_Store
._*

# Backups
/*.sql

/.env.local
/.env.*.local
/CLAUDE.md
